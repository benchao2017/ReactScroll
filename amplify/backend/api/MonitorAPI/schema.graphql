type UserActivity @model {
  id: ID!
  phone: String!
  cursorPosition: String
}

type EmailTemplate @model {
  id: ID!
  name: String!
  subject: String
  htmlBody: String
  textBody: String
  files: AWSJSON
}
